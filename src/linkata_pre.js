/*
const linkata_container = document.getElementById("listener");
const linkata_menu_left = document.getElementById("linkata-menu-left");
const linkata_menu_right = document.getElementById("linkata-menu-right");
const linkata_menu_floating = document.getElementById("linkata-menu-floating");

setInterval(
    function () {
        const interval_ms = 1000;
        const time_ms = Date.now();
        const progress = time_ms / interval_ms;
        const posx = 200 + (Math.sin(progress * Math.PI * 2) * 100);
        const posy = 200 + (Math.cos(progress * Math.PI * 2) * 100);
        linkata_menu_floating.style.left = posx + "px";
        linkata_menu_floating.style.top = posy + "px";
    },
    10
);
*/